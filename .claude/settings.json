{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "powershell -NoProfile -Command \"Set-Location C:\\\\Users\\\\asus\\\\Documents\\\\niranjan-portfolio; git add .; $diff = git diff --cached --name-only; if ($diff) { $ts = Get-Date -Format yyyy-MM-dd_HH:mm; git commit -m \\\\\"Claude: $ts\\\\\"; git push origin HEAD }\""
          }
        ]
      }
    ]
  }
}
